import Calculator from '@/components/Calculator';

const HomePage = () => {
  return (
    <div className="flex flex-col h-full min-w-screen">
      <Calculator />
    </div>
  );
};

export default HomePage;
